server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  prometheus:
    metrics:
      export:
        enabled: true

webclient:
  timeout:
    connection: 5000
    response: 10000
    read: 30000

spring:
  application:
    name: individuals_api
  security:
    oauth2:
      client:
        provider:
          keycloak:
            token-uri: http://localhost:8080/realms/orchestrator/protocol/openid-connect/token
            authorization-uri: http://localhost:8080/admin/realms/orchestrator/users
        registration:
          keycloak:
            provider: keycloak
            client-id: orchestrator
            client-secret: UXZLFcKOlgakfTy12nff8ZaJ2LmNcOCo
            client-name: orchestrator
            authorization-grant-type: authorization_code
            scope:
              - openid
              - profile
              - email
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/orchestrator